# PlayMatch 채팅 개발기 2부: STOMP 도입과 좌충우돌 버그 수정기

## 들어가며

지난 1부에서는 기본적인 채팅방 생성 및 메시지 전송 기능을 구현했다. 하지만 이 방식은 서버가 1대일 때만 동작하며, 실시간성이 보장되지 않는 한계가 있었다. 이번 2부에서는 STOMP 프로토콜을 도입하여 실시간 채팅 기능을 고도화하고, 그 과정에서 마주친 수많은 버그들을 해결해나간 여정을 기록한다.

## 1. STOMP와 WebSocket, 그리고 Redis (Redis 연동 완료)

- **문제점:** 기존 채팅은 단순 HTTP 폴링 방식의 한계.
- **해결책:** 실시간 양방향 통신을 위해 WebSocket과 STOMP 프로토콜 도입을 결정.
- **완료된 작업:** 사용자가 늘어나 서버를 여러 대로 확장(Scale-out)할 경우를 대비하여, Redis를 메시지 브로커로 사용하여 서버 간 메시지를 중계하는 아키텍처를 성공적으로 구현했습니다. Redis의 Pub/Sub 기능을 활용하여 메시지를 발행(publish)하고 구독(subscribe)하며, `RedisTemplate`을 통해 메시지 객체를 JSON 형태로 직렬화하여 전송합니다. `RedisSubscriber`가 Redis 채널을 구독하고 WebSocket 클라이언트에게 메시지를 브로드캐스트하는 방식으로 동작합니다.

## 2. 끝없는 버그와의 사투: 디버깅의 기록

STOMP를 도입하고 기존 기능을 통합하는 과정에서, 예상치 못한 수많은 버그와 마주쳤다. 이 과정을 상세히 기록한다.

- **``403 Forbidden`` 미스터리:** 경기 생성, 메시지 전송 등 대부분의 기능이 권한 없음(403) 에러를 반환했다. 하지만 실제 원인은 인증 문제가 아니었다.
    - **원인 1: 데이터 불일치 (Payload Mismatch):** 프론트엔드가 보내는 데이터 객체와 백엔드가 기대하는 DTO(`Data Transfer Object`)의 필드명이 달라 발생한 유효성 검사(`Validation`) 실패가 403 에러로 잘못 표기되고 있었다.
    - **원인 2: 프로토콜 불일치 (Protocol Mismatch):** 프론트엔드는 Raw WebSocket으로, 백엔드는 STOMP 프로토콜로 통신을 시도하여 서로의 메시지를 이해하지 못했다. 이로 인해 WebSocket 연결이 실패하고, 구현되지 않은 HTTP API로 요청이 넘어가며 에러가 발생했다.

- **프론트엔드 호환성 문제:**
    - **`global is not defined`:** `sockjs-client` 라이브러리가 최신 빌드 도구인 Vite 환경과 호환되지 않아 발생. `vite.config.ts`에 `define: { global: 'window' }` 설정을 추가하여 해결.
    - **`is not a function`:** STOMP 기반으로 `chatService`를 새로 작성하면서, 기존 UI 컴포넌트들이 의존하던 옛날 함수(`onRoomsUpdate` 등)들이 사라져 발생. 새 서비스의 API에 맞게 UI 컴포넌트들을 모두 수정하여 해결.

- **백엔드 컴파일 및 런타임 에러:**
    - **`cannot find symbol`:** `Principal`, `Authentication`, `LocalDateTime` 등 다수의 클래스를 사용하면서 `import` 구문을 누락하여 발생. 수차례에 걸쳐 수정.
    - **타입 불일치 (Incompatible Types):** User ID를 `Integer`와 `Long` 타입으로 혼용하여 발생. 관련된 모든 DTO와 엔티티, 서비스의 타입을 `Integer`로 통일하여 해결.
    - **``NullPointerException`` / ``IllegalStateException``:** STOMP 환경에서 메시지(`SEND`)를 처리하는 스레드에 인증 정보가 전달되지 않는 문제. ``StompHandler``가 `CONNECT` 시점에 세션에 저장한 인증 정보를, 컨트롤러가 ``SimpMessageHeaderAccessor``를 통해 직접 가져오도록 수정하여 해결.

- **최종 UI 버그:**
    - **'알 수 없는 채팅방':** 1:1 채팅방 생성 시, 백엔드에서 채팅방 이름을 상대방 이름으로 설정하는 로직이 누락되어 발생. `ChatRoom` 엔티티에 `name` 필드를 추가하고, ``ChatService``에서 이름을 설정하도록 수정하여 해결.
    - **데이터 필드명 불일치:** API 응답과 프론트엔드 타입 간의 필드명 불일치(`roomId` vs `id`, `message` vs `content`)로 인해 UI에 정보가 표시되지 않는 문제. 백엔드 `DTO`와 프론트엔드 타입을 모두 수정하여 최종적으로 해결.

## 4. 실시간 알림 기능 구현

단순히 채팅방에 있을 때만 메시지를 보는 것을 넘어, 사용자가 다른 페이지에 있더라도 새로운 메시지가 왔음을 인지할 수 있도록 알림 기능을 추가했다.

- **백엔드:**
    - `ChatRoomMember`에 `lastReadAt` 필드를 추가하여 사용자가 마지막으로 읽은 시간을 기록.
    - `POST /api/chat/rooms/{roomId}/read` API를 구현하여, 사용자가 채팅방에 들어오면 읽음 상태를 업데이트.
    - `GET /api/chat/unread-count` API를 구현하여, 전체 안 읽은 메시지 개수를 계산.
- **프론트엔드:**
    - `NavBar` 컴포넌트에서 주기적으로(Polling) '안 읽은 메시지 개수' API를 호출.
    - 안 읽은 메시지가 있으면 '채팅' 메뉴 옆에 빨간 뱃지를 표시.

## 4. 결론

수많은 버그를 해결하며 경기 생성부터, 추천 매치 목록에서 주최자에게 연락하기, 실시간 채팅, 그리고 알림 기능까지 성공적으로 구현했습니다. 특히 Redis를 메시지 브로커로 활용하여 채팅 시스템을 고도화하고, 복잡했던 테스트 환경을 안정화하여 모든 테스트가 통과하는 견고한 기반을 마련했습니다. 이 과정은 단순히 기능을 만드는 것을 넘어, 프론트엔드와 백엔드 간의 정확한 데이터 통신, 인증 방식의 이해, 그리고 아키텍처의 중요성을 다시 한번 깨닫게 된 계기가 되었습니다.

이제 PlayMatch의 채팅 시스템은 실시간성과 확장성을 모두 갖추게 되었으며, 안정적인 테스트 환경 위에서 지속적인 기능 개선이 가능해졌습니다. 다음 단계로는 추가적인 기능 고도화와 성능 최적화에 집중할 예정입니다.
